// project import
import product from './product';
import dashboard from './dashboard';
import category from './category';
import support from './support';

// ==============================|| MENU ITEMS ||============================== //

const menuItems = {
    items: [dashboard, product, category, support]
};

export default menuItems;
